DATABASE_URL=postgresql://postgres:postgres@localhost:5432/videochat
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
WHISPER_MODEL=base
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Optional: path to a YouTube cookies.txt (Netscape format), needed on
# datacenter/VPS hosts where yt-dlp gets bot-checked by YouTube.
# COOKIES_FILE=/app/cookies.txt
